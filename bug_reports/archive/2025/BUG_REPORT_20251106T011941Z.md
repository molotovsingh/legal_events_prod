# Bug Report — “Recent Runs” Section Lists Cases, Not Runs (Misleading UI)

Date: 2025-11-06
Timestamp: 2025-11-06T01:19:41Z (UTC)
Author: Codex CLI assistant

## Summary

The “Recent Runs” section loads clients and cases and renders a placeholder “View Case” button, but doesn’t display runs. The section title is misleading and provides no direct run visibility.

## Evidence
- frontend/index.html:329–357 — Iterates clients → cases, and renders placeholder `viewCase()` which alerts “Feature coming soon!”

## Impact
- Users cannot see actual runs from the “Recent Runs” panel, reducing usability and debuggability.

## Recommended Fixes

Option A (preferred): Add API route and UI rendering for runs by case
- Implement `/v1/cases/{case_id}/runs` or similar, and render recent runs (status, counts, started_at) with links to details.

Option B: Rename and clarify
- Temporarily change panel title to “Browse Cases” and add a note that runs view is coming soon.

## Validation Steps
1) Load the page after creating runs; runs list should populate with real data.
2) Clicking a run should show status and link to events/export.

