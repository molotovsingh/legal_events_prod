# Bug Report — Anthropic SDK Integration Audit (Tool Use & JSON Output)

Date: 2025-11-06
Timestamp: 2025-11-06T03:37:03Z (UTC)
Author: Codex CLI assistant

## Summary

Anthropic’s Python SDK has changed significantly (2024–2025) in tool use and structured output. To ensure robust JSON outputs and avoid unintended tool calls, audit `core/anthropic_adapter.py` and update patterns accordingly.

## Evidence
- core/anthropic_adapter.py: implements Claude adapter (tool use likely).

Key upstream changes:
- `tool_choice={"type": "none"}` to prevent further tool calls when sending tool results.
- Tool result ordering requirements: tool_result blocks must precede text.
- Emerging helpers for tool execution (SDK 0.68.0+) and document content blocks in tool_result (0.66.0).

## Impact
- Without updates, structured JSON may be inconsistent; tool calls may chain unexpectedly.

## Recommended Fixes
1) Force JSON via tool pattern:
   - Define JSON schema as a tool; set `tool_choice={"type": "tool", "name": "json"}` for guaranteed structure.
2) Prevent unintended tools:
   - When returning results, use `tool_choice={"type": "none"}`.
3) Ordering & parsing:
   - Ensure tool_result blocks are first; extract JSON from `tool_use.input`.
4) Version pinning:
   - Pin anthropic SDK to a tested version; capture supported features in adapter.

## Validation Steps
1) Run extraction on sample docs; verify strict JSON shape across providers.
2) Add unit tests for tool choice behavior and result parsing.

