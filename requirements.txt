# Core Dependencies for Legal Events v2
# Python 3.12+

# FastAPI and Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.6
sse-starlette>=1.6.5

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Storage (MinIO)
minio>=7.2.0

# Queue (Redis/RQ)
redis>=5.0.0
rq>=1.15.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0

# Data Processing (pinned for Python 3.12 compatibility)
pandas==2.1.4
openpyxl>=3.1.5,<4.0.0  # docling 2.60.1 requires >=3.1.5
numpy<2

# Existing Pipeline Dependencies
# (These are needed to run the extraction pipeline)
docling==2.60.1
anthropic>=0.40.0,<1.0.0  # Tool choice API stable, prevent breaking changes
openai>=1.0.0
google-generativeai>=0.8.0
requests>=2.32.0
python-dateutil>=2.9.0

# Tokenizers for accurate token counting (pre-run + post-run)
# v0.12.0: Provider-specific token counting with graceful fallback
tiktoken>=0.7.0  # REQUIRED for GPT models (OpenAI/OpenRouter) - ~5MB
# OPTIONAL: Remove transformers+sentencepiece if you only use GPT/Claude models
# Llama models require transformers (~1.2GB) - raises clear error if missing
transformers>=4.45.0  # Optional: Llama tokenization (meta-llama/* models)
sentencepiece>=0.1.99  # Required by transformers for Llama tokenizers

# Document Processing
PyMuPDF>=1.24.0  # For PDF text extraction (fitz module)
extract-msg>=0.48.0  # For MS Office document parsing (MSG, MSG files)

# Event Extraction (LangExtract for Gemini support)
# Installed from git (not PyPI) to ensure latest bug fixes and features
langextract @ git+https://github.com/google/langextract.git

# Development/Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# Monitoring (optional)
prometheus-client>=0.19.0
